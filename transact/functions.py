# PLC Functions
from transact.models import Transaction

def download(controller_ipaddress, controller_slot, data, user):
    
    # to start, create a new transact record
    t = Transaction(user)


def upload(controller_ipaddress, controller_slot):
    
    t = Transaction(user)